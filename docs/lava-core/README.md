# lava-core

`lava-core` 提供 Java 基础工具类，包括重试、IO 操作、网络工具等，底层基于 Guava 实现。

## 引入依赖

如果你已经通过 BOM 管理版本，只需引入 `lava-core`.

```xml
<dependency>
    <groupId>com.zhengshuyun</groupId>
    <artifactId>lava-core</artifactId>
</dependency>
```