# 文档编写规范

## 代码示例

- 文档中的 Java 示例聚焦当前章节的关键调用和业务流程，默认省略 `package`、`import`、外围类和无关样板代码。
- 常见 JDK 类型和模块公开类型不需要通过 `import` 解释，开发者可根据类型名自行补全。
- 只有类型同名、来源存在歧义，或导入方式本身就是当前章节要说明的内容时，才展示必要的 `import`。
- 示例不必为了独立复制编译而补齐无关上下文，但 API 名称、参数单位、失败语义和关键安全边界必须准确。
